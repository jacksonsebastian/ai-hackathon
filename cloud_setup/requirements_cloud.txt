# ============================================================
# JUPYTER CLOUD GPU REQUIREMENTS (ROCm Compatible)
# ============================================================
# Install these ONLY on the cloud GPU machine.
# Uses transformers pipeline for Whisper (ROCm compatible).
# Does NOT use faster_whisper/ctranslate2 (NVIDIA CUDA only).
# ============================================================

# Whisper Large V3 via transformers (already installed with vLLM)
# transformers  — already installed
# torch         — already installed

# Kokoro TTS (Text-to-Speech)
kokoro>=0.8
soundfile
scipy

# BGE-M3 Embeddings
sentence-transformers>=3.0.0

# API Server
fastapi
uvicorn[standard]

# Utilities
numpy
